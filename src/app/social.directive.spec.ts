import { SocialDirective } from './social.directive';

describe('SocialDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialDirective();
    expect(directive).toBeTruthy();
  });
});
